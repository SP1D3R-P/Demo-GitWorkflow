services:
  my_app:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Tell your app to use the magic DNS name
      - OLLAMA_URL=http://host.docker.internal:11434
      - OLLAMA_HOST=http://host.docker.internal:11434
    
