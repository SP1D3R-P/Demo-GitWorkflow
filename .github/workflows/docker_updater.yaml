name: Docker Updater
on: 
  push:
    paths:
      - src/**

jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
      # saw that docker comes with linux 
      - name: Docker Building 
        run: docker build -t my-app:latest .
